import styles from "./Profile.module.css";

const Profile = () => (
	<div className={styles.profile}>
		<h1>Mon profil</h1>
		<form className={styles.formulaire}></form>
	</div>
);

export default Profile;
